# Codex Prompt: Update TAK Server Documentation, Runbooks, and Demo Notes

You are working in the `bdaoud90/tak-ops` repository.

Your task is to make a comprehensive documentation update that captures the current TAK Server build/debug state, the root causes already discovered, the fixes already applied, the current successful MVP server state, and the next-step client testing workflow for WinTAK and CivTAK.

Do not make speculative code changes to the TAK runtime itself unless clearly required for documentation accuracy. This task is primarily documentation and operational runbook work. Prefer additive, well-structured documentation changes over broad rewrites.

## High-level objective

Document the operational reality of the current TAK 5.7 Ubuntu 24.04 deployment so that another operator can:

1. Understand the architecture and failure modes encountered.
2. Reproduce the troubleshooting path if needed.
3. Understand the current known-good state.
4. Bring the server to a demo-ready state.
5. Test connectivity from a Windows TAK client and from CivTAK on Android.

## Current known environment and facts to encode in docs

### Host / platform
- Ubuntu 24.04 on a DigitalOcean droplet.
- TAK Server 5.7 package-based deployment under `/opt/tak`.
- Init script at `/etc/init.d/takserver` orchestrates the following services:
  - `takserver-config`
  - `takserver-messaging`
  - `takserver-api`
  - `takserver-plugins`
  - `takserver-retention`
- Main wrapper service behavior can be misleading because subservices may partially start while the stack is still unhealthy.

### PostgreSQL facts
- PostgreSQL 15 is used.
- The real cluster state must be checked with `pg_lsclusters`, not only `systemctl status postgresql`.
- Prior blocker: PostgreSQL cluster was down even while wrapper service looked superficially OK.
- Prior blocker: PostgreSQL startup was broken by memory settings; lowering memory-related settings restored cluster startup.
- Verified successful state now includes:
  - PostgreSQL cluster online
  - database `cot` accessible
  - `martiuser` can connect successfully to `cot`
- `SHOW max_connections;` as `postgres` returns `100`.
- `martiuser` was elevated to `SUPERUSER` during troubleshooting to avoid metadata/pool-sizing access problems.

### TAK cert / keystore facts
- Multiple earlier failures were caused by stale CA artifacts, password mismatch, and misuse of the cert scripts.
- Important failure mode: old CA key + new password produced misleading downstream TLS symptoms.
- Important operational lesson: delete the stale CA and generated artifacts before regenerating if passwords drifted.
- Cert generation eventually succeeded.
- Current certificate/keystore artifacts exist under `/opt/tak/certs/files/`.
- Verified generated artifacts include at least:
  - `takserver.jks`
  - `takserver.p12`
  - `truststore-root.jks`
  - `fed-truststore.jks`
  - `admin.p12`
  - `user.p12`
- `keytool -list -keystore /opt/tak/certs/files/takserver.jks` succeeds using the actual configured password.
- Cert artifacts were generated by root but are readable by the `tak` user.
- Messaging log contains a non-blocking warning that TLS is enabled but no CRL/OCSP validation is configured.

### CoreConfig facts
Document the effective/current state of `CoreConfig.xml` and the key lessons from it:
- Network ports in use:
  - `8089` for TLS ingest (`stdssl`)
  - `8090` for `quic`
  - `8443` HTTPS
  - `8444` federation HTTPS
  - `8446` cert HTTPS
- The `<repository>` connection points at:
  - JDBC URL: `jdbc:postgresql://127.0.0.1:5432/cot`
  - user: `martiuser`
- Important lesson: keystore/truststore passwords in `CoreConfig.xml` must match the password used when generating certs.
- Important lesson: `keymanager` casing matters (`SunX509`, not malformed variants).
- Important lesson: localhost references should consistently use `127.0.0.1` unless there is a deliberate reason otherwise.

### Service/logging facts
- TAK debugging required looking at per-service logs under `/opt/tak/logs`, not just `systemctl`/`journalctl`.
- Useful logs include:
  - `takserver-config.log`
  - `takserver-api.log`
  - `takserver-messaging.log`
  - `takserver-plugins.log`
  - `takserver-retention.log`
- Plugin service produced noisy stack traces earlier (`PluginService.canAccessPluginDataFeedApi(...)`) and was suspected as a destabilizing component during MVP troubleshooting.
- Current confirmed good state from logs:
  - `takserver-api.log` shows Tomcat initialized with ports `8443`, `8444`, and `8446`.
  - `takserver-messaging.log` shows normal startup and TLS warning only.
  - `takserver-retention.log` shows normal startup and DB pool startup.
- Current confirmed good listener state from `ss -ltnp`:
  - `0.0.0.0:8446`
  - `0.0.0.0:8443`
  - `0.0.0.0:8089`

### Current operational conclusion
- The server has crossed from “install/debug chaos” into “demo-ready MVP server baseline.”
- Core listeners are up.
- The next phase is client enrollment and connectivity testing.

## Documentation work to perform

Create or update documentation so the repository clearly reflects the above.

### 1. Update `README.md`
Add or revise sections that cover:
- Current TAK deployment status.
- Service architecture (multi-service stack, not a single monolith).
- Key verified listeners now online:
  - 8443
  - 8446
  - 8089
- Summary of the major issues encountered and resolved:
  - PostgreSQL cluster vs wrapper-service confusion
  - PostgreSQL memory tuning on a small droplet
  - stale CA artifacts / password mismatch
  - keystore/truststore password alignment in `CoreConfig.xml`
  - importance of checking `/opt/tak/logs/*` by component
- Current known non-blocking warnings:
  - no CRL/OCSP validation
  - plugin service produced earlier instability/noise and should be treated carefully in MVP testing

Keep the README concise but concrete. Do not turn it into a full incident diary.

### 2. Add `docs/runbooks/tak-demo-mvp.md`
Create a detailed demo runbook that includes:

#### Goal
Bring the server to a state where a Windows TAK client and CivTAK on Android can be tested.

#### Server-side verification checklist
Include commands and expected output patterns for:
- PostgreSQL cluster state
- DB reachability as `martiuser`
- presence of keystore/truststore/client `.p12` files
- listener verification with `ss -ltnp`
- checking key logs under `/opt/tak/logs`

#### Key troubleshooting lessons
Document:
- Use `pg_lsclusters` for truth, not only `systemctl status postgresql`
- Cert generation scripts require careful environment handling; stale CA artifacts break regeneration
- `CoreConfig.xml` password mismatches silently block listeners
- Per-service logs matter more than wrapper service status

#### Demo-ready current state
Explicitly note that a known-good state now includes these listeners:
- `8443`
- `8446`
- `8089`

#### Windows client test procedure
Document a concrete test flow for WinTAK:
- copy `admin.p12` or `user.p12` from `/opt/tak/certs/files/`
- import the certificate in WinTAK
- configure server using public IP `178.62.235.44`
- start with port `8446` as the primary client-facing cert-auth endpoint
- note that `8443` is the web/UI path and `8089` is TLS ingest, so `8446` is the best first direct client test
- describe what to watch server-side during connection attempts (`tail -f` logs / `ss -tnp`)

#### Android CivTAK test procedure
Document:
- move `user.p12` to the device
- import certificate into CivTAK
- configure server using public IP `178.62.235.44`
- start with `8446`
- validate successful connection and attempt a simple marker/position update

#### Post-demo hardening backlog
Add a section for later improvements:
- CRL/OCSP
- least-privilege DB access instead of `SUPERUSER`
- revisit plugins/federation once MVP path is stable
- audit `CoreConfig.xml` for unnecessary complexity

### 3. Add `docs/known-issues.md`
Create or update a known issues document that captures:
- Stale CA/private key artifacts can poison later cert generation.
- `sudo` environment handling can break cert scripts by dropping required variables.
- PostgreSQL wrapper service can look “up” while the actual cluster is down.
- On small droplets, PostgreSQL memory settings may prevent startup.
- Plugin service may introduce noisy or confusing failures during startup.
- TLS may be active without CRL/OCSP and that should be tracked as a security hardening issue.

Each issue should have:
- symptom
- likely cause
- verification command(s)
- fix or mitigation

### 4. Add `docs/architecture/tak-service-map.md`
Create a document explaining the service layout discovered from `/etc/init.d/takserver`.
Include:
- wrapper script starts multiple services
- what each service appears to do at a high level:
  - config
  - messaging
  - api
  - plugins
  - retention
- why debugging needs to be done per microservice
- which logs map to which service
- how port binding correlates to service health

### 5. Add `docs/timeline/tak-debugging-checkpoint-2026-04-12.md`
Create a timestamped checkpoint document that summarizes the debugging campaign without being bloated.
Include:
- initial state
- major blockers encountered
- key pivots that actually moved the problem forward
- final successful checkpoint where ports `8443`, `8446`, and `8089` were confirmed listening
- current next step: client validation

Keep it factual, operational, and useful for future operators.

## Style requirements
- Use a professional, operator-oriented tone.
- Prefer precise, structured prose over generic explanation.
- Distinguish clearly between:
  - verified facts
  - current known-good state
  - known issues
  - future hardening items
- Do not exaggerate certainty where the notes are only operationally inferred.
- Avoid fluff.

## Deliverable expectations
Make the documentation changes directly in the repository.
If a file already exists, update it intelligently rather than replacing useful content blindly.
At the end, provide a concise summary of:
- files changed
- key additions
- any assumptions you had to make
