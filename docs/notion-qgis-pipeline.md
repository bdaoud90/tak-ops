# Notion → QGIS Data Pipeline

Utilities under `tooling/notion` and `tooling/geo` support:
1. Exporting or ingesting report records.
2. Normalizing fields into a GIS-friendly schema.
3. Converting CSV to GeoJSON.
4. Validating GeoJSON features before publishing.

These helpers are intentionally generic and suitable for pilot-level workflows.
